---
name: project-inventory-ui-icons
description: "ForestInventoryUI HUD tweaks — gold pill moved to top-center, item gear icons are stylized vector mini-pictures"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4b3aab28-3ee4-407e-b7db-bc11dd243ebc
---

ForestInventoryUI (StarterPlayer.StarterPlayerScripts, cloned live into Players.<name>.PlayerScripts) UI changes, 2026-06-24:

- HUD gold pill moved from top-left to **top-center** (AnchorPoint 0.5,0; Position 0.5,0,0,8) so the Roblox topbar/menu button no longer covers it.
- Added `makeItemIcon(parent, id, data, kind, sizePx)` helper (above `gearSlot`) that draws a distinct stylized emblem per item from nested Frames via a `px(...)` shape helper — no image assets exist for items, only `color` + shield `visual` blocks in [[project_rebirth_shop_build]] GameData. Per-id art: CandyCane (striped cane), LollipopHammer (pink swirl), ChocolateBlade (sword), CrystalScepter (gem scepter); shields draw a crest from `visual.face/rim/style/emblemColor`; cosmetics get a sparkle; fallback gem.
- `gearSlot` (BAG, incl. equipped item) and shop catalog rows both call `makeItemIcon` instead of the old flat colored circle/square. BAG icon 38px, SHOP icon 52px.

Also disabled Roblox's default Backpack/hotbar GUI (`StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Backpack,false)` next to the Health one at top of script). Reason: the held-weapon hotbar slot showed a blank gray box because `Tool.TextureId` is empty on the `ServerStorage.Weapons.<name>` templates (cloned by PlayerInit.giveWeapon, see [[project_weapon_swap_fix]]), and TextureId can ONLY take an uploaded image asset — it cannot render frame-based vector icons. No item images exist in the project, so user chose to hide the hotbar entirely; BAG menu is now the sole gear UI. Weapon still equips/works.

Verified in playtest: compiles clean (only the usual DataStore StudioAccessToApisNotAllowed warning), icons render in both BAG and SHOP, gold centered, hotbar slot gone. See [[project_hub_systems]] for related HUD/monetization work.

2026-07-03: Replaced the old bottom-left plain text buttons (REBIRTH/SHOP/BAG hudButton stack) with a **left-side stylized icon button stack** named `SideButtons` (Frame, AnchorPoint 0,0.5 at left-center) holding 4 TextButtons: ShopBtn (candy basket), BagBtn (backpack), DailySpinBtn (prize wheel + pointer), RebirthBtn (circular arrows + gold spark) — order top→bottom: Shop, Bag, Daily Spin, Rebirth. Icons are pure vector Frames via `mk(...)` shape helper + `arcH(...)` (half-circle arc = circular UIStroke clipped top/bottom; NOTE: rotated ClipsDescendants frames don't clip in Roblox, so only axis-aligned half arcs work). Labels: FredokaOne white + dark UIStroke, per-button UIScale hover/press pop tweens. Var names shopBtn/bagBtn/rebirthBtn kept so all old Activated connections still work; keybinds P/R/B unchanged. New: `wheelBtn.Activated` → closeAll() + fires `PlayerGui.WheelGui.OpenWheelUI` BindableEvent; WheelUI creates that BindableEvent (after dim/close connections) and toggles open/close — wheel is now openable anywhere from HUD, not just the hub WheelPad kiosk prompt. All 4 buttons verified in playtest 2026-07-03 (each opens its panel; shop/reb/bag stay one-at-a-time).

2026-07-03 (later): **Shop panel redesigned in "Grow a Garden" style** from user-supplied GrowAGardenExampleGUI.rbxm (copy kept in project folder root). Studio's `game:GetObjects("file://...")` refuses local files ("Invalid local file") — worked around by writing a pure-Python rbxm binary parser (LZ4 block decompress + INST/PROP/PRNT chunks; script in session outputs as parse_rbxm.py) to extract the example's structure. New shop look (all in ForestInventoryUI, same `shop`/`shopClose`/`refreshShop`/`shopRows` API + BuyItem logic untouched): scale-based square panel UDim2.fromScale(0.7,0.7)+UIAspectRatioConstraint(1), chocolate bg + 5px black Border UIStroke, TopBar (Name="TopBar" not set in script — script leaves default names) with white FredokaOne outlined "CANDY SHOP", yellow $gold pill (255,204,0, dark-gold border, ratio 4), red X (253,29,29, dark-red double stroke, ratio 1, Name unset). Cards: rarity-tinted (RARITY_COL darkened 0.42) sharp-corner frames, ratio 4.2, 4px black border, icon square left (makeItemIcon 64px), Enum.Font.Cartoon texts (= example's ComicNeueAngular) with Contextual black UIStrokes, rarity tag top-right, FredokaOne buy button (ratio 3.5, corner 0.15, text+border strokes). Helpers added: textStroke/borderStroke/darkenC. Gotcha: UIAspectRatioConstraint(FitWithinMaxSize) collapses if one size axis is scale 0 — rows need explicit (0.94,0.22) scale. Verified in play: renders, BuyItem→OWNED refresh works, X closes.

2026-07-03 (later still): Shop scrollbar + PNG icon prep. (1) **Scrollbar gotcha**: AutomaticCanvasSize ignores Scale-sized children, so the shop canvas never overflowed and no scrollbar appeared. Fix: rows are offset-sized by `layoutShopRows()` (rowW=(scrollW-24)*0.96, rowH=rowW/4.2, re-run on AbsoluteSize change); scrollbar is 16px amber (255,204,0), VerticalScrollBarInset Always. (2) **ICON_ASSETS table** at top of ForestInventoryUI: keys Shop/DailySpin/Rebirth/Money, empty string = vector fallback icon. User sent 4 PNG icons (storefront, pinwheel, pink/blue circular arrows, money stack) pasted inline (no files) — every programmatic upload path is blocked in this Studio (GetObjects file:// = "Invalid local file", upload_image = http/https only, CreateAssetAsync = "not available yet"), so user must upload via Asset Manager/Creator Hub and supply rbxassetid's; then fill ICON_ASSETS. DONE — user uploaded and IDs are live in ICON_ASSETS: Shop=rbxassetid://91186555432940 (storefront), DailySpin=rbxassetid://83814565385571 (pinwheel), Rebirth=rbxassetid://113510400677770 (pink/blue arrows), Money=rbxassetid://76150699624364 (money stack). Verified in playtest 2026-07-03: PNG icons render on side buttons, top pill shows money icon + number with no "$". Bag=rbxassetid://136565575181042 (brown satchel PNG) added 2026-07-03 — all 4 side buttons now use PNGs; vector fallbacks remain in code behind `if not hasImg`. iconButton() takes optional assetId (ImageLabel ScaleType.Fit) and returns hasImage; vector draws wrapped in `if not hasImg`. Top gold pill: GOLD_PREFIX = "" when Money icon set (adds 48px ImageLabel left of text, text shifts right), "$" otherwise; bag pill keeps "$".

2026-07-03: **HP bar restyled** to match free model 1628158147 "Simple Health Bar GUI" (user request; inserted via insert_asset to study, then deleted from ReplicatedStorage). Same spot (bottom-right above minimap, 210x30): black 0.25-transparency track, corner 8, 5px Contextual UIStroke tinted col:Lerp(black,0.75), fill tweens width 0.25s Linear, colors HP_FULL (0,200,50) >85% / HP_MID amber 35-85% / HP_LOW red <35% with 0.1s color tweens on fill+stroke, centered SourceSans "N%" text (percentage now, not "cur / max") with 3px black text stroke. bindChar upd() rewritten accordingly. Verified: green 100%, amber 79%; red not visually caught (out-of-combat regen ~6%/s outpaces screenshots) but same code path. InsertService:LoadAsset errors "not authorized" for foreign free models — use insert_asset MCP tool instead.

2026-06-24 follow-up: Rebuilt the **Lollipop Hammer 3D model** (ServerStorage.Weapons."Lollipop Hammer") to match the round-lollipop icon. Was a candy-cane shape (brown Handle stick + 5 pink cube segments climbing it + 8 pink balls forming a hook curl, all WeldConstraint'd to Handle). Now: kept Handle (0.42x3.4x0.42 brown stick) + scripts (ClientAttack, ShieldRig) + Grip (unchanged) + RequiresHandle; deleted all old candy parts/welds; added LollipopHead (Cylinder, Size (0.5,2.2,2.2) so round faces point along handle local X / sideways, NO rotation — a flat disc), 4 concentric Swirl cylinders (white/deep-pink, nudged along +X), and a StickCap ball at the bottom (local y=-1.7). Reattached each with a fresh WeldConstraint(Part0=Handle). Gotcha learned: Roblox Cylinder's circular faces are on its LOCAL X axis; do NOT rotate it to face sideways. Icon reverted to original round-lollipop (disc+swirl+stick). If user wants a 3D-round head instead of flat disc, swap LollipopHead to a Ball.
